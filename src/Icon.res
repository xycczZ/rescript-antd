
        module StepBackwardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StepBackwardOutlined"
        }
        
        module StepForwardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StepForwardOutlined"
        }
        
        module FastBackwardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FastBackwardOutlined"
        }
        
        module FastForwardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FastForwardOutlined"
        }
        
        module ShrinkOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShrinkOutlined"
        }
        
        module ArrowsAltOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ArrowsAltOutlined"
        }
        
        module DownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownOutlined"
        }
        
        module UpOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpOutlined"
        }
        
        module LeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftOutlined"
        }
        
        module RightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightOutlined"
        }
        
        module CaretUpOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretUpOutlined"
        }
        
        module CaretDownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretDownOutlined"
        }
        
        module CaretLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretLeftOutlined"
        }
        
        module CaretRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretRightOutlined"
        }
        
        module UpCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpCircleOutlined"
        }
        
        module DownCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownCircleOutlined"
        }
        
        module LeftCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftCircleOutlined"
        }
        
        module RightCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightCircleOutlined"
        }
        
        module DoubleRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DoubleRightOutlined"
        }
        
        module DoubleLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DoubleLeftOutlined"
        }
        
        module VerticalLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VerticalLeftOutlined"
        }
        
        module VerticalRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VerticalRightOutlined"
        }
        
        module VerticalAlignTopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VerticalAlignTopOutlined"
        }
        
        module VerticalAlignMiddleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VerticalAlignMiddleOutlined"
        }
        
        module VerticalAlignBottomOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VerticalAlignBottomOutlined"
        }
        
        module ForwardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ForwardOutlined"
        }
        
        module BackwardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BackwardOutlined"
        }
        
        module RollbackOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RollbackOutlined"
        }
        
        module EnterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EnterOutlined"
        }
        
        module RetweetOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RetweetOutlined"
        }
        
        module SwapOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SwapOutlined"
        }
        
        module SwapLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SwapLeftOutlined"
        }
        
        module SwapRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SwapRightOutlined"
        }
        
        module ArrowUpOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ArrowUpOutlined"
        }
        
        module ArrowDownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ArrowDownOutlined"
        }
        
        module ArrowLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ArrowLeftOutlined"
        }
        
        module ArrowRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ArrowRightOutlined"
        }
        
        module PlayCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlayCircleOutlined"
        }
        
        module UpSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpSquareOutlined"
        }
        
        module DownSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownSquareOutlined"
        }
        
        module LeftSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftSquareOutlined"
        }
        
        module RightSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightSquareOutlined"
        }
        
        module LoginOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LoginOutlined"
        }
        
        module LogoutOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LogoutOutlined"
        }
        
        module MenuFoldOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MenuFoldOutlined"
        }
        
        module MenuUnfoldOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MenuUnfoldOutlined"
        }
        
        module BorderBottomOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderBottomOutlined"
        }
        
        module BorderHorizontalOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderHorizontalOutlined"
        }
        
        module BorderInnerOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderInnerOutlined"
        }
        
        module BorderOuterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderOuterOutlined"
        }
        
        module BorderLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderLeftOutlined"
        }
        
        module BorderRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderRightOutlined"
        }
        
        module BorderTopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderTopOutlined"
        }
        
        module BorderVerticleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderVerticleOutlined"
        }
        
        module PicCenterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PicCenterOutlined"
        }
        
        module PicLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PicLeftOutlined"
        }
        
        module PicRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PicRightOutlined"
        }
        
        module RadiusBottomleftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RadiusBottomleftOutlined"
        }
        
        module RadiusBottomrightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RadiusBottomrightOutlined"
        }
        
        module RadiusUpleftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RadiusUpleftOutlined"
        }
        
        module RadiusUprightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RadiusUprightOutlined"
        }
        
        module FullscreenOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FullscreenOutlined"
        }
        
        module FullscreenExitOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FullscreenExitOutlined"
        }
        
        module QuestionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QuestionOutlined"
        }
        
        module QuestionCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QuestionCircleOutlined"
        }
        
        module PlusOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusOutlined"
        }
        
        module PlusCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusCircleOutlined"
        }
        
        module PauseOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PauseOutlined"
        }
        
        module PauseCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PauseCircleOutlined"
        }
        
        module MinusOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusOutlined"
        }
        
        module MinusCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusCircleOutlined"
        }
        
        module PlusSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusSquareOutlined"
        }
        
        module MinusSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusSquareOutlined"
        }
        
        module InfoOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InfoOutlined"
        }
        
        module InfoCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InfoCircleOutlined"
        }
        
        module ExclamationOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExclamationOutlined"
        }
        
        module ExclamationCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExclamationCircleOutlined"
        }
        
        module CloseOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseOutlined"
        }
        
        module CloseCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseCircleOutlined"
        }
        
        module CloseSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseSquareOutlined"
        }
        
        module CheckOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckOutlined"
        }
        
        module CheckCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckCircleOutlined"
        }
        
        module CheckSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckSquareOutlined"
        }
        
        module ClockCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ClockCircleOutlined"
        }
        
        module WarningOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WarningOutlined"
        }
        
        module IssuesCloseOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IssuesCloseOutlined"
        }
        
        module StopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StopOutlined"
        }
        
        module EditOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EditOutlined"
        }
        
        module FormOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FormOutlined"
        }
        
        module CopyOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CopyOutlined"
        }
        
        module ScissorOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ScissorOutlined"
        }
        
        module DeleteOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeleteOutlined"
        }
        
        module SnippetsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SnippetsOutlined"
        }
        
        module DiffOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DiffOutlined"
        }
        
        module HighlightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HighlightOutlined"
        }
        
        module AlignCenterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlignCenterOutlined"
        }
        
        module AlignLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlignLeftOutlined"
        }
        
        module AlignRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlignRightOutlined"
        }
        
        module BgColorsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BgColorsOutlined"
        }
        
        module BoldOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BoldOutlined"
        }
        
        module ItalicOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ItalicOutlined"
        }
        
        module UnderlineOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UnderlineOutlined"
        }
        
        module StrikethroughOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StrikethroughOutlined"
        }
        
        module RedoOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedoOutlined"
        }
        
        module UndoOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UndoOutlined"
        }
        
        module ZoomInOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ZoomInOutlined"
        }
        
        module ZoomOutOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ZoomOutOutlined"
        }
        
        module FontColorsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FontColorsOutlined"
        }
        
        module FontSizeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FontSizeOutlined"
        }
        
        module LineHeightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LineHeightOutlined"
        }
        
        module DashOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DashOutlined"
        }
        
        module SmallDashOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SmallDashOutlined"
        }
        
        module SortAscendingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SortAscendingOutlined"
        }
        
        module SortDescendingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SortDescendingOutlined"
        }
        
        module DragOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DragOutlined"
        }
        
        module OrderedListOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "OrderedListOutlined"
        }
        
        module UnorderedListOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UnorderedListOutlined"
        }
        
        module RadiusSettingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RadiusSettingOutlined"
        }
        
        module ColumnWidthOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ColumnWidthOutlined"
        }
        
        module ColumnHeightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ColumnHeightOutlined"
        }
        
        module AreaChartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AreaChartOutlined"
        }
        
        module PieChartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PieChartOutlined"
        }
        
        module BarChartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BarChartOutlined"
        }
        
        module DotChartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DotChartOutlined"
        }
        
        module LineChartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LineChartOutlined"
        }
        
        module RadarChartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RadarChartOutlined"
        }
        
        module HeatMapOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HeatMapOutlined"
        }
        
        module FallOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FallOutlined"
        }
        
        module RiseOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RiseOutlined"
        }
        
        module StockOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StockOutlined"
        }
        
        module BoxPlotOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BoxPlotOutlined"
        }
        
        module FundOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FundOutlined"
        }
        
        module SlidersOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlidersOutlined"
        }
        
        module AndroidOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AndroidOutlined"
        }
        
        module AppleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AppleOutlined"
        }
        
        module WindowsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WindowsOutlined"
        }
        
        module IeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IeOutlined"
        }
        
        module ChromeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ChromeOutlined"
        }
        
        module GithubOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GithubOutlined"
        }
        
        module AliwangwangOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AliwangwangOutlined"
        }
        
        module DingdingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DingdingOutlined"
        }
        
        module WeiboSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WeiboSquareOutlined"
        }
        
        module WeiboCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WeiboCircleOutlined"
        }
        
        module TaobaoCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TaobaoCircleOutlined"
        }
        
        module Html5Outlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "Html5Outlined"
        }
        
        module WeiboOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WeiboOutlined"
        }
        
        module TwitterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TwitterOutlined"
        }
        
        module WechatOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WechatOutlined"
        }
        
        module YoutubeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "YoutubeOutlined"
        }
        
        module AlipayCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlipayCircleOutlined"
        }
        
        module TaobaoOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TaobaoOutlined"
        }
        
        module SkypeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SkypeOutlined"
        }
        
        module QqOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QqOutlined"
        }
        
        module MediumWorkmarkOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MediumWorkmarkOutlined"
        }
        
        module GitlabOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GitlabOutlined"
        }
        
        module MediumOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MediumOutlined"
        }
        
        module LinkedinOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LinkedinOutlined"
        }
        
        module GooglePlusOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GooglePlusOutlined"
        }
        
        module DropboxOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DropboxOutlined"
        }
        
        module FacebookOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FacebookOutlined"
        }
        
        module CodepenOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodepenOutlined"
        }
        
        module CodeSandboxOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodeSandboxOutlined"
        }
        
        module AmazonOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AmazonOutlined"
        }
        
        module GoogleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoogleOutlined"
        }
        
        module CodepenCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodepenCircleOutlined"
        }
        
        module AlipayOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlipayOutlined"
        }
        
        module AntDesignOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AntDesignOutlined"
        }
        
        module AntCloudOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AntCloudOutlined"
        }
        
        module AliyunOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AliyunOutlined"
        }
        
        module ZhihuOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ZhihuOutlined"
        }
        
        module SlackOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlackOutlined"
        }
        
        module SlackSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlackSquareOutlined"
        }
        
        module BehanceOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BehanceOutlined"
        }
        
        module BehanceSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BehanceSquareOutlined"
        }
        
        module DribbbleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DribbbleOutlined"
        }
        
        module DribbbleSquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DribbbleSquareOutlined"
        }
        
        module InstagramOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InstagramOutlined"
        }
        
        module YuqueOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "YuqueOutlined"
        }
        
        module AlibabaOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlibabaOutlined"
        }
        
        module YahooOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "YahooOutlined"
        }
        
        module RedditOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedditOutlined"
        }
        
        module SketchOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SketchOutlined"
        }
        
        module AccountBookOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AccountBookOutlined"
        }
        
        module AimOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AimOutlined"
        }
        
        module AlertOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlertOutlined"
        }
        
        module ApartmentOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ApartmentOutlined"
        }
        
        module ApiOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ApiOutlined"
        }
        
        module AppstoreAddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AppstoreAddOutlined"
        }
        
        module AppstoreOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AppstoreOutlined"
        }
        
        module AudioOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AudioOutlined"
        }
        
        module AudioMutedOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AudioMutedOutlined"
        }
        
        module AuditOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AuditOutlined"
        }
        
        module BankOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BankOutlined"
        }
        
        module BarcodeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BarcodeOutlined"
        }
        
        module BarsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BarsOutlined"
        }
        
        module BellOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BellOutlined"
        }
        
        module BlockOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BlockOutlined"
        }
        
        module BookOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BookOutlined"
        }
        
        module BorderOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderOutlined"
        }
        
        module BorderlessTableOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BorderlessTableOutlined"
        }
        
        module BranchesOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BranchesOutlined"
        }
        
        module BugOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BugOutlined"
        }
        
        module BuildOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BuildOutlined"
        }
        
        module BulbOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BulbOutlined"
        }
        
        module CalculatorOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CalculatorOutlined"
        }
        
        module CalendarOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CalendarOutlined"
        }
        
        module CameraOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CameraOutlined"
        }
        
        module CarOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CarOutlined"
        }
        
        module CarryOutOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CarryOutOutlined"
        }
        
        module CiCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CiCircleOutlined"
        }
        
        module CiOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CiOutlined"
        }
        
        module ClearOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ClearOutlined"
        }
        
        module CloudDownloadOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudDownloadOutlined"
        }
        
        module CloudOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudOutlined"
        }
        
        module CloudServerOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudServerOutlined"
        }
        
        module CloudSyncOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudSyncOutlined"
        }
        
        module CloudUploadOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudUploadOutlined"
        }
        
        module ClusterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ClusterOutlined"
        }
        
        module CodeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodeOutlined"
        }
        
        module CoffeeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CoffeeOutlined"
        }
        
        module CommentOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CommentOutlined"
        }
        
        module CompassOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CompassOutlined"
        }
        
        module CompressOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CompressOutlined"
        }
        
        module ConsoleSqlOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ConsoleSqlOutlined"
        }
        
        module ContactsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ContactsOutlined"
        }
        
        module ContainerOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ContainerOutlined"
        }
        
        module ControlOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ControlOutlined"
        }
        
        module CopyrightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CopyrightOutlined"
        }
        
        module CreditCardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CreditCardOutlined"
        }
        
        module CrownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CrownOutlined"
        }
        
        module CustomerServiceOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CustomerServiceOutlined"
        }
        
        module DashboardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DashboardOutlined"
        }
        
        module DatabaseOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DatabaseOutlined"
        }
        
        module DeleteColumnOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeleteColumnOutlined"
        }
        
        module DeleteRowOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeleteRowOutlined"
        }
        
        module DeliveredProcedureOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeliveredProcedureOutlined"
        }
        
        module DeploymentUnitOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeploymentUnitOutlined"
        }
        
        module DesktopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DesktopOutlined"
        }
        
        module DingtalkOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DingtalkOutlined"
        }
        
        module DisconnectOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DisconnectOutlined"
        }
        
        module DislikeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DislikeOutlined"
        }
        
        module DollarCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DollarCircleOutlined"
        }
        
        module DollarOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DollarOutlined"
        }
        
        module DownloadOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownloadOutlined"
        }
        
        module EllipsisOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EllipsisOutlined"
        }
        
        module EnvironmentOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EnvironmentOutlined"
        }
        
        module EuroCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EuroCircleOutlined"
        }
        
        module EuroOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EuroOutlined"
        }
        
        module ExceptionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExceptionOutlined"
        }
        
        module ExpandAltOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExpandAltOutlined"
        }
        
        module ExpandOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExpandOutlined"
        }
        
        module ExperimentOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExperimentOutlined"
        }
        
        module ExportOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExportOutlined"
        }
        
        module EyeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EyeOutlined"
        }
        
        module EyeInvisibleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EyeInvisibleOutlined"
        }
        
        module FieldBinaryOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FieldBinaryOutlined"
        }
        
        module FieldNumberOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FieldNumberOutlined"
        }
        
        module FieldStringOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FieldStringOutlined"
        }
        
        module FieldTimeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FieldTimeOutlined"
        }
        
        module FileAddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileAddOutlined"
        }
        
        module FileDoneOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileDoneOutlined"
        }
        
        module FileExcelOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileExcelOutlined"
        }
        
        module FileExclamationOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileExclamationOutlined"
        }
        
        module FileOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileOutlined"
        }
        
        module FileGifOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileGifOutlined"
        }
        
        module FileImageOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileImageOutlined"
        }
        
        module FileJpgOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileJpgOutlined"
        }
        
        module FileMarkdownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileMarkdownOutlined"
        }
        
        module FilePdfOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilePdfOutlined"
        }
        
        module FilePptOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilePptOutlined"
        }
        
        module FileProtectOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileProtectOutlined"
        }
        
        module FileSearchOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileSearchOutlined"
        }
        
        module FileSyncOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileSyncOutlined"
        }
        
        module FileTextOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileTextOutlined"
        }
        
        module FileUnknownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileUnknownOutlined"
        }
        
        module FileWordOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileWordOutlined"
        }
        
        module FileZipOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileZipOutlined"
        }
        
        module FilterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilterOutlined"
        }
        
        module FireOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FireOutlined"
        }
        
        module FlagOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FlagOutlined"
        }
        
        module FolderAddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderAddOutlined"
        }
        
        module FolderOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderOutlined"
        }
        
        module FolderOpenOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderOpenOutlined"
        }
        
        module FolderViewOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderViewOutlined"
        }
        
        module ForkOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ForkOutlined"
        }
        
        module FormatPainterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FormatPainterOutlined"
        }
        
        module FrownOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FrownOutlined"
        }
        
        module FunctionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FunctionOutlined"
        }
        
        module FundProjectionScreenOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FundProjectionScreenOutlined"
        }
        
        module FundViewOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FundViewOutlined"
        }
        
        module FunnelPlotOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FunnelPlotOutlined"
        }
        
        module GatewayOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GatewayOutlined"
        }
        
        module GifOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GifOutlined"
        }
        
        module GiftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GiftOutlined"
        }
        
        module GlobalOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GlobalOutlined"
        }
        
        module GoldOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoldOutlined"
        }
        
        module GroupOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GroupOutlined"
        }
        
        module HddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HddOutlined"
        }
        
        module HeartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HeartOutlined"
        }
        
        module HistoryOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HistoryOutlined"
        }
        
        module HolderOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HolderOutlined"
        }
        
        module HomeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HomeOutlined"
        }
        
        module HourglassOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HourglassOutlined"
        }
        
        module IdcardOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IdcardOutlined"
        }
        
        module ImportOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ImportOutlined"
        }
        
        module InboxOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InboxOutlined"
        }
        
        module InsertRowAboveOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsertRowAboveOutlined"
        }
        
        module InsertRowBelowOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsertRowBelowOutlined"
        }
        
        module InsertRowLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsertRowLeftOutlined"
        }
        
        module InsertRowRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsertRowRightOutlined"
        }
        
        module InsuranceOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsuranceOutlined"
        }
        
        module InteractionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InteractionOutlined"
        }
        
        module KeyOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "KeyOutlined"
        }
        
        module LaptopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LaptopOutlined"
        }
        
        module LayoutOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LayoutOutlined"
        }
        
        module LikeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LikeOutlined"
        }
        
        module LineOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LineOutlined"
        }
        
        module LinkOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LinkOutlined"
        }
        
        module Loading3QuartersOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "Loading3QuartersOutlined"
        }
        
        module LoadingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LoadingOutlined"
        }
        
        module LockOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LockOutlined"
        }
        
        module MacCommandOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MacCommandOutlined"
        }
        
        module MailOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MailOutlined"
        }
        
        module ManOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ManOutlined"
        }
        
        module MedicineBoxOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MedicineBoxOutlined"
        }
        
        module MehOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MehOutlined"
        }
        
        module MenuOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MenuOutlined"
        }
        
        module MergeCellsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MergeCellsOutlined"
        }
        
        module MessageOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MessageOutlined"
        }
        
        module MobileOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MobileOutlined"
        }
        
        module MoneyCollectOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MoneyCollectOutlined"
        }
        
        module MonitorOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MonitorOutlined"
        }
        
        module MoreOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MoreOutlined"
        }
        
        module NodeCollapseOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NodeCollapseOutlined"
        }
        
        module NodeExpandOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NodeExpandOutlined"
        }
        
        module NodeIndexOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NodeIndexOutlined"
        }
        
        module NotificationOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NotificationOutlined"
        }
        
        module NumberOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NumberOutlined"
        }
        
        module OneToOneOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "OneToOneOutlined"
        }
        
        module PaperClipOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PaperClipOutlined"
        }
        
        module PartitionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PartitionOutlined"
        }
        
        module PayCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PayCircleOutlined"
        }
        
        module PercentageOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PercentageOutlined"
        }
        
        module PhoneOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PhoneOutlined"
        }
        
        module PictureOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PictureOutlined"
        }
        
        module PlaySquareOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlaySquareOutlined"
        }
        
        module PoundCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PoundCircleOutlined"
        }
        
        module PoundOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PoundOutlined"
        }
        
        module PoweroffOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PoweroffOutlined"
        }
        
        module PrinterOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PrinterOutlined"
        }
        
        module ProfileOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ProfileOutlined"
        }
        
        module ProjectOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ProjectOutlined"
        }
        
        module PropertySafetyOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PropertySafetyOutlined"
        }
        
        module PullRequestOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PullRequestOutlined"
        }
        
        module PushpinOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PushpinOutlined"
        }
        
        module QrcodeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QrcodeOutlined"
        }
        
        module ReadOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ReadOutlined"
        }
        
        module ReconciliationOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ReconciliationOutlined"
        }
        
        module RedEnvelopeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedEnvelopeOutlined"
        }
        
        module ReloadOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ReloadOutlined"
        }
        
        module RestOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RestOutlined"
        }
        
        module RobotOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RobotOutlined"
        }
        
        module RocketOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RocketOutlined"
        }
        
        module RotateLeftOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RotateLeftOutlined"
        }
        
        module RotateRightOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RotateRightOutlined"
        }
        
        module SafetyCertificateOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SafetyCertificateOutlined"
        }
        
        module SafetyOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SafetyOutlined"
        }
        
        module SaveOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SaveOutlined"
        }
        
        module ScanOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ScanOutlined"
        }
        
        module ScheduleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ScheduleOutlined"
        }
        
        module SearchOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SearchOutlined"
        }
        
        module SecurityScanOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SecurityScanOutlined"
        }
        
        module SelectOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SelectOutlined"
        }
        
        module SendOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SendOutlined"
        }
        
        module SettingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SettingOutlined"
        }
        
        module ShakeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShakeOutlined"
        }
        
        module ShareAltOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShareAltOutlined"
        }
        
        module ShopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShopOutlined"
        }
        
        module ShoppingCartOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShoppingCartOutlined"
        }
        
        module ShoppingOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShoppingOutlined"
        }
        
        module SisternodeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SisternodeOutlined"
        }
        
        module SkinOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SkinOutlined"
        }
        
        module SmileOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SmileOutlined"
        }
        
        module SolutionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SolutionOutlined"
        }
        
        module SoundOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SoundOutlined"
        }
        
        module SplitCellsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SplitCellsOutlined"
        }
        
        module StarOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StarOutlined"
        }
        
        module SubnodeOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SubnodeOutlined"
        }
        
        module SwitcherOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SwitcherOutlined"
        }
        
        module SyncOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SyncOutlined"
        }
        
        module TableOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TableOutlined"
        }
        
        module TabletOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TabletOutlined"
        }
        
        module TagOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TagOutlined"
        }
        
        module TagsOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TagsOutlined"
        }
        
        module TeamOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TeamOutlined"
        }
        
        module ThunderboltOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ThunderboltOutlined"
        }
        
        module ToTopOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ToTopOutlined"
        }
        
        module ToolOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ToolOutlined"
        }
        
        module TrademarkCircleOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrademarkCircleOutlined"
        }
        
        module TrademarkOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrademarkOutlined"
        }
        
        module TransactionOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TransactionOutlined"
        }
        
        module TranslationOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TranslationOutlined"
        }
        
        module TrophyOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrophyOutlined"
        }
        
        module UngroupOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UngroupOutlined"
        }
        
        module UnlockOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UnlockOutlined"
        }
        
        module UploadOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UploadOutlined"
        }
        
        module UsbOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UsbOutlined"
        }
        
        module UserAddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UserAddOutlined"
        }
        
        module UserDeleteOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UserDeleteOutlined"
        }
        
        module UserOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UserOutlined"
        }
        
        module UserSwitchOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UserSwitchOutlined"
        }
        
        module UsergroupAddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UsergroupAddOutlined"
        }
        
        module UsergroupDeleteOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UsergroupDeleteOutlined"
        }
        
        module VerifiedOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VerifiedOutlined"
        }
        
        module VideoCameraAddOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VideoCameraAddOutlined"
        }
        
        module VideoCameraOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VideoCameraOutlined"
        }
        
        module WalletOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WalletOutlined"
        }
        
        module WhatsAppOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WhatsAppOutlined"
        }
        
        module WifiOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WifiOutlined"
        }
        
        module WomanOutlined = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WomanOutlined"
        }
        
        
        module StepBackwardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StepBackwardFilled"
        }
        
        
        module StepForwardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StepForwardFilled"
        }
        
        
        module FastBackwardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FastBackwardFilled"
        }
        
        
        module FastForwardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FastForwardFilled"
        }
        
        
        module CaretUpFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretUpFilled"
        }
        
        
        module CaretDownFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretDownFilled"
        }
        
        
        module CaretLeftFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretLeftFilled"
        }
        
        
        module CaretRightFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CaretRightFilled"
        }
        
        
        module UpCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpCircleFilled"
        }
        
        
        module DownCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownCircleFilled"
        }
        
        
        module LeftCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftCircleFilled"
        }
        
        
        module RightCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightCircleFilled"
        }
        
        
        module ForwardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ForwardFilled"
        }
        
        
        module BackwardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BackwardFilled"
        }
        
        
        module PlayCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlayCircleFilled"
        }
        
        
        module UpSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpSquareFilled"
        }
        
        
        module DownSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownSquareFilled"
        }
        
        
        module LeftSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftSquareFilled"
        }
        
        
        module RightSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightSquareFilled"
        }
        
        
        module QuestionCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QuestionCircleFilled"
        }
        
        
        module PlusCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusCircleFilled"
        }
        
        
        module PauseCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PauseCircleFilled"
        }
        
        
        module MinusCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusCircleFilled"
        }
        
        
        module PlusSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusSquareFilled"
        }
        
        
        module MinusSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusSquareFilled"
        }
        
        
        module InfoCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InfoCircleFilled"
        }
        
        
        module ExclamationCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExclamationCircleFilled"
        }
        
        
        module CloseCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseCircleFilled"
        }
        
        
        module CloseSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseSquareFilled"
        }
        
        
        module CheckCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckCircleFilled"
        }
        
        
        module CheckSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckSquareFilled"
        }
        
        
        module ClockCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ClockCircleFilled"
        }
        
        
        module WarningFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WarningFilled"
        }
        
        
        module StopFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StopFilled"
        }
        
        
        module EditFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EditFilled"
        }
        
        
        module CopyFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CopyFilled"
        }
        
        
        module DeleteFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeleteFilled"
        }
        
        
        module SnippetsFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SnippetsFilled"
        }
        
        
        module DiffFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DiffFilled"
        }
        
        
        module HighlightFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HighlightFilled"
        }
        
        
        module PieChartFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PieChartFilled"
        }
        
        
        module BoxPlotFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BoxPlotFilled"
        }
        
        
        module FundFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FundFilled"
        }
        
        
        module SlidersFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlidersFilled"
        }
        
        
        module AndroidFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AndroidFilled"
        }
        
        
        module AppleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AppleFilled"
        }
        
        
        module WindowsFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WindowsFilled"
        }
        
        
        module ChromeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ChromeFilled"
        }
        
        
        module GithubFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GithubFilled"
        }
        
        
        module AliwangwangFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AliwangwangFilled"
        }
        
        
        module WeiboSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WeiboSquareFilled"
        }
        
        
        module WeiboCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WeiboCircleFilled"
        }
        
        
        module TaobaoCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TaobaoCircleFilled"
        }
        
        
        module Html5Filled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "Html5Filled"
        }
        
        
        module WechatFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WechatFilled"
        }
        
        
        module YoutubeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "YoutubeFilled"
        }
        
        
        module AlipayCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlipayCircleFilled"
        }
        
        
        module SkypeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SkypeFilled"
        }
        
        
        module GitlabFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GitlabFilled"
        }
        
        
        module LinkedinFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LinkedinFilled"
        }
        
        
        module FacebookFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FacebookFilled"
        }
        
        
        module CodeSandboxCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodeSandboxCircleFilled"
        }
        
        
        module CodepenCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodepenCircleFilled"
        }
        
        
        module SlackSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlackSquareFilled"
        }
        
        
        module BehanceSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BehanceSquareFilled"
        }
        
        
        module DribbbleSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DribbbleSquareFilled"
        }
        
        
        module InstagramFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InstagramFilled"
        }
        
        
        module YuqueFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "YuqueFilled"
        }
        
        
        module YahooFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "YahooFilled"
        }
        
        
        module AccountBookFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AccountBookFilled"
        }
        
        
        module AlertFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlertFilled"
        }
        
        
        module AlipaySquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlipaySquareFilled"
        }
        
        
        module AmazonCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AmazonCircleFilled"
        }
        
        
        module AmazonSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AmazonSquareFilled"
        }
        
        
        module ApiFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ApiFilled"
        }
        
        
        module AppstoreFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AppstoreFilled"
        }
        
        
        module AudioFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AudioFilled"
        }
        
        
        module BankFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BankFilled"
        }
        
        
        module BehanceCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BehanceCircleFilled"
        }
        
        
        module BellFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BellFilled"
        }
        
        
        module BookFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BookFilled"
        }
        
        
        module BugFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BugFilled"
        }
        
        
        module BuildFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BuildFilled"
        }
        
        
        module BulbFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BulbFilled"
        }
        
        
        module CalculatorFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CalculatorFilled"
        }
        
        
        module CalendarFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CalendarFilled"
        }
        
        
        module CameraFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CameraFilled"
        }
        
        
        module CarFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CarFilled"
        }
        
        
        module CarryOutFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CarryOutFilled"
        }
        
        
        module CiCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CiCircleFilled"
        }
        
        
        module CloudFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudFilled"
        }
        
        
        module CodeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodeFilled"
        }
        
        
        module CodeSandboxSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodeSandboxSquareFilled"
        }
        
        
        module CodepenSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodepenSquareFilled"
        }
        
        
        module CompassFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CompassFilled"
        }
        
        
        module ContactsFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ContactsFilled"
        }
        
        
        module ContainerFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ContainerFilled"
        }
        
        
        module ControlFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ControlFilled"
        }
        
        
        module CreditCardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CreditCardFilled"
        }
        
        
        module CrownFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CrownFilled"
        }
        
        
        module CustomerServiceFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CustomerServiceFilled"
        }
        
        
        module DashboardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DashboardFilled"
        }
        
        
        module DatabaseFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DatabaseFilled"
        }
        
        
        module DingtalkCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DingtalkCircleFilled"
        }
        
        
        module DingtalkSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DingtalkSquareFilled"
        }
        
        
        module DislikeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DislikeFilled"
        }
        
        
        module DollarCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DollarCircleFilled"
        }
        
        
        module DribbbleCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DribbbleCircleFilled"
        }
        
        
        module DropboxCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DropboxCircleFilled"
        }
        
        
        module DropboxSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DropboxSquareFilled"
        }
        
        
        module EnvironmentFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EnvironmentFilled"
        }
        
        
        module EuroCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EuroCircleFilled"
        }
        
        
        module ExperimentFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExperimentFilled"
        }
        
        
        module EyeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EyeFilled"
        }
        
        
        module EyeInvisibleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EyeInvisibleFilled"
        }
        
        
        module FileAddFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileAddFilled"
        }
        
        
        module FileExcelFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileExcelFilled"
        }
        
        
        module FileExclamationFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileExclamationFilled"
        }
        
        
        module FileFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileFilled"
        }
        
        
        module FileImageFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileImageFilled"
        }
        
        
        module FileMarkdownFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileMarkdownFilled"
        }
        
        
        module FilePdfFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilePdfFilled"
        }
        
        
        module FilePptFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilePptFilled"
        }
        
        
        module FileTextFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileTextFilled"
        }
        
        
        module FileUnknownFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileUnknownFilled"
        }
        
        
        module FileWordFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileWordFilled"
        }
        
        
        module FileZipFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileZipFilled"
        }
        
        
        module FilterFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilterFilled"
        }
        
        
        module FireFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FireFilled"
        }
        
        
        module FlagFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FlagFilled"
        }
        
        
        module FolderAddFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderAddFilled"
        }
        
        
        module FolderFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderFilled"
        }
        
        
        module FolderOpenFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderOpenFilled"
        }
        
        
        module FormatPainterFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FormatPainterFilled"
        }
        
        
        module FrownFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FrownFilled"
        }
        
        
        module FunnelPlotFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FunnelPlotFilled"
        }
        
        
        module GiftFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GiftFilled"
        }
        
        
        module GoldFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoldFilled"
        }
        
        
        module GoldenFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoldenFilled"
        }
        
        
        module GoogleCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoogleCircleFilled"
        }
        
        
        module GooglePlusCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GooglePlusCircleFilled"
        }
        
        
        module GooglePlusSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GooglePlusSquareFilled"
        }
        
        
        module GoogleSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoogleSquareFilled"
        }
        
        
        module HddFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HddFilled"
        }
        
        
        module HeartFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HeartFilled"
        }
        
        
        module HomeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HomeFilled"
        }
        
        
        module HourglassFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HourglassFilled"
        }
        
        
        module IdcardFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IdcardFilled"
        }
        
        
        module IeCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IeCircleFilled"
        }
        
        
        module IeSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IeSquareFilled"
        }
        
        
        module InsuranceFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsuranceFilled"
        }
        
        
        module InteractionFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InteractionFilled"
        }
        
        
        module LayoutFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LayoutFilled"
        }
        
        
        module LikeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LikeFilled"
        }
        
        
        module LockFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LockFilled"
        }
        
        
        module MacCommandFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MacCommandFilled"
        }
        
        
        module MailFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MailFilled"
        }
        
        
        module MedicineBoxFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MedicineBoxFilled"
        }
        
        
        module MediumCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MediumCircleFilled"
        }
        
        
        module MediumSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MediumSquareFilled"
        }
        
        
        module MehFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MehFilled"
        }
        
        
        module MessageFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MessageFilled"
        }
        
        
        module MobileFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MobileFilled"
        }
        
        
        module MoneyCollectFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MoneyCollectFilled"
        }
        
        
        module NotificationFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NotificationFilled"
        }
        
        
        module PayCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PayCircleFilled"
        }
        
        
        module PhoneFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PhoneFilled"
        }
        
        
        module PictureFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PictureFilled"
        }
        
        
        module PlaySquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlaySquareFilled"
        }
        
        
        module PoundCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PoundCircleFilled"
        }
        
        
        module PrinterFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PrinterFilled"
        }
        
        
        module ProfileFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ProfileFilled"
        }
        
        
        module ProjectFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ProjectFilled"
        }
        
        
        module PropertySafetyFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PropertySafetyFilled"
        }
        
        
        module PushpinFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PushpinFilled"
        }
        
        
        module QqCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QqCircleFilled"
        }
        
        
        module QqSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QqSquareFilled"
        }
        
        
        module ReadFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ReadFilled"
        }
        
        
        module ReconciliationFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ReconciliationFilled"
        }
        
        
        module RedEnvelopeFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedEnvelopeFilled"
        }
        
        
        module RedditCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedditCircleFilled"
        }
        
        
        module RedditSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedditSquareFilled"
        }
        
        
        module RestFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RestFilled"
        }
        
        
        module RobotFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RobotFilled"
        }
        
        
        module RocketFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RocketFilled"
        }
        
        
        module SafetyCertificateFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SafetyCertificateFilled"
        }
        
        
        module SaveFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SaveFilled"
        }
        
        
        module ScheduleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ScheduleFilled"
        }
        
        
        module SecurityScanFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SecurityScanFilled"
        }
        
        
        module SettingFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SettingFilled"
        }
        
        
        module ShopFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShopFilled"
        }
        
        
        module ShoppingFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShoppingFilled"
        }
        
        
        module SignalFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SignalFilled"
        }
        
        
        module SketchCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SketchCircleFilled"
        }
        
        
        module SketchSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SketchSquareFilled"
        }
        
        
        module SkinFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SkinFilled"
        }
        
        
        module SlackCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlackCircleFilled"
        }
        
        
        module SmileFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SmileFilled"
        }
        
        
        module SoundFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SoundFilled"
        }
        
        
        module StarFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StarFilled"
        }
        
        
        module SwitcherFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SwitcherFilled"
        }
        
        
        module TabletFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TabletFilled"
        }
        
        
        module TagFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TagFilled"
        }
        
        
        module TagsFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TagsFilled"
        }
        
        
        module TaobaoSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TaobaoSquareFilled"
        }
        
        
        module ThunderboltFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ThunderboltFilled"
        }
        
        
        module ToolFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ToolFilled"
        }
        
        
        module TrademarkCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrademarkCircleFilled"
        }
        
        
        module TrophyFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrophyFilled"
        }
        
        
        module TwitterCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TwitterCircleFilled"
        }
        
        
        module TwitterSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TwitterSquareFilled"
        }
        
        
        module UnlockFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UnlockFilled"
        }
        
        
        module UsbFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UsbFilled"
        }
        
        
        module VideoCameraFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VideoCameraFilled"
        }
        
        
        module WalletFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WalletFilled"
        }
        
        
        module ZhihuCircleFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ZhihuCircleFilled"
        }
        
        
        module ZhihuSquareFilled = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ZhihuSquareFilled"
        }
        
        
        module UpCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpCircleTwoTone"
        }
        
        
        module DownCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownCircleTwoTone"
        }
        
        
        module LeftCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftCircleTwoTone"
        }
        
        
        module RightCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightCircleTwoTone"
        }
        
        
        module PlayCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlayCircleTwoTone"
        }
        
        
        module UpSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UpSquareTwoTone"
        }
        
        
        module DownSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DownSquareTwoTone"
        }
        
        
        module LeftSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LeftSquareTwoTone"
        }
        
        
        module RightSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RightSquareTwoTone"
        }
        
        
        module QuestionCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "QuestionCircleTwoTone"
        }
        
        
        module PlusCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusCircleTwoTone"
        }
        
        
        module PauseCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PauseCircleTwoTone"
        }
        
        
        module MinusCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusCircleTwoTone"
        }
        
        
        module PlusSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlusSquareTwoTone"
        }
        
        
        module MinusSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MinusSquareTwoTone"
        }
        
        
        module InfoCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InfoCircleTwoTone"
        }
        
        
        module ExclamationCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExclamationCircleTwoTone"
        }
        
        
        module CloseCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseCircleTwoTone"
        }
        
        
        module CloseSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloseSquareTwoTone"
        }
        
        
        module CheckCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckCircleTwoTone"
        }
        
        
        module CheckSquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CheckSquareTwoTone"
        }
        
        
        module ClockCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ClockCircleTwoTone"
        }
        
        
        module WarningTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WarningTwoTone"
        }
        
        
        module StopTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StopTwoTone"
        }
        
        
        module EditTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EditTwoTone"
        }
        
        
        module CopyTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CopyTwoTone"
        }
        
        
        module DeleteTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DeleteTwoTone"
        }
        
        
        module SnippetsTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SnippetsTwoTone"
        }
        
        
        module DiffTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DiffTwoTone"
        }
        
        
        module HighlightTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HighlightTwoTone"
        }
        
        
        module PieChartTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PieChartTwoTone"
        }
        
        
        module BoxPlotTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BoxPlotTwoTone"
        }
        
        
        module FundTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FundTwoTone"
        }
        
        
        module SlidersTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SlidersTwoTone"
        }
        
        
        module Html5TwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "Html5TwoTone"
        }
        
        
        module AccountBookTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AccountBookTwoTone"
        }
        
        
        module AlertTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AlertTwoTone"
        }
        
        
        module ApiTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ApiTwoTone"
        }
        
        
        module AppstoreTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AppstoreTwoTone"
        }
        
        
        module AudioTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "AudioTwoTone"
        }
        
        
        module BankTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BankTwoTone"
        }
        
        
        module BellTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BellTwoTone"
        }
        
        
        module BookTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BookTwoTone"
        }
        
        
        module BugTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BugTwoTone"
        }
        
        
        module BuildTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BuildTwoTone"
        }
        
        
        module BulbTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "BulbTwoTone"
        }
        
        
        module CalculatorTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CalculatorTwoTone"
        }
        
        
        module CalendarTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CalendarTwoTone"
        }
        
        
        module CameraTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CameraTwoTone"
        }
        
        
        module CarTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CarTwoTone"
        }
        
        
        module CarryOutTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CarryOutTwoTone"
        }
        
        
        module CiCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CiCircleTwoTone"
        }
        
        
        module CiTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CiTwoTone"
        }
        
        
        module CloudTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CloudTwoTone"
        }
        
        
        module CodeTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CodeTwoTone"
        }
        
        
        module CompassTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CompassTwoTone"
        }
        
        
        module ContactsTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ContactsTwoTone"
        }
        
        
        module ContainerTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ContainerTwoTone"
        }
        
        
        module ControlTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ControlTwoTone"
        }
        
        
        module CopyrightTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CopyrightTwoTone"
        }
        
        
        module CreditCardTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CreditCardTwoTone"
        }
        
        
        module CrownTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CrownTwoTone"
        }
        
        
        module CustomerServiceTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "CustomerServiceTwoTone"
        }
        
        
        module DashboardTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DashboardTwoTone"
        }
        
        
        module DatabaseTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DatabaseTwoTone"
        }
        
        
        module DislikeTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DislikeTwoTone"
        }
        
        
        module DollarCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DollarCircleTwoTone"
        }
        
        
        module DollarTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "DollarTwoTone"
        }
        
        
        module EnvironmentTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EnvironmentTwoTone"
        }
        
        
        module EuroCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EuroCircleTwoTone"
        }
        
        
        module EuroTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EuroTwoTone"
        }
        
        
        module ExperimentTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ExperimentTwoTone"
        }
        
        
        module EyeTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EyeTwoTone"
        }
        
        
        module EyeInvisibleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "EyeInvisibleTwoTone"
        }
        
        
        module FileAddTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileAddTwoTone"
        }
        
        
        module FileExcelTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileExcelTwoTone"
        }
        
        
        module FileExclamationTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileExclamationTwoTone"
        }
        
        
        module FileTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileTwoTone"
        }
        
        
        module FileImageTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileImageTwoTone"
        }
        
        
        module FileMarkdownTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileMarkdownTwoTone"
        }
        
        
        module FilePdfTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilePdfTwoTone"
        }
        
        
        module FilePptTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilePptTwoTone"
        }
        
        
        module FileTextTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileTextTwoTone"
        }
        
        
        module FileUnknownTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileUnknownTwoTone"
        }
        
        
        module FileWordTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileWordTwoTone"
        }
        
        
        module FileZipTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FileZipTwoTone"
        }
        
        
        module FilterTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FilterTwoTone"
        }
        
        
        module FireTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FireTwoTone"
        }
        
        
        module FlagTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FlagTwoTone"
        }
        
        
        module FolderAddTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderAddTwoTone"
        }
        
        
        module FolderTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderTwoTone"
        }
        
        
        module FolderOpenTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FolderOpenTwoTone"
        }
        
        
        module FrownTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FrownTwoTone"
        }
        
        
        module FunnelPlotTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "FunnelPlotTwoTone"
        }
        
        
        module GiftTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GiftTwoTone"
        }
        
        
        module GoldTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "GoldTwoTone"
        }
        
        
        module HddTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HddTwoTone"
        }
        
        
        module HeartTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HeartTwoTone"
        }
        
        
        module HomeTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HomeTwoTone"
        }
        
        
        module HourglassTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "HourglassTwoTone"
        }
        
        
        module IdcardTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "IdcardTwoTone"
        }
        
        
        module InsuranceTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InsuranceTwoTone"
        }
        
        
        module InteractionTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "InteractionTwoTone"
        }
        
        
        module LayoutTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LayoutTwoTone"
        }
        
        
        module LikeTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LikeTwoTone"
        }
        
        
        module LockTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "LockTwoTone"
        }
        
        
        module MailTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MailTwoTone"
        }
        
        
        module MedicineBoxTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MedicineBoxTwoTone"
        }
        
        
        module MehTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MehTwoTone"
        }
        
        
        module MessageTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MessageTwoTone"
        }
        
        
        module MobileTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MobileTwoTone"
        }
        
        
        module MoneyCollectTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "MoneyCollectTwoTone"
        }
        
        
        module NotificationTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "NotificationTwoTone"
        }
        
        
        module PhoneTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PhoneTwoTone"
        }
        
        
        module PictureTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PictureTwoTone"
        }
        
        
        module PlaySquareTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PlaySquareTwoTone"
        }
        
        
        module PoundCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PoundCircleTwoTone"
        }
        
        
        module PrinterTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PrinterTwoTone"
        }
        
        
        module ProfileTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ProfileTwoTone"
        }
        
        
        module ProjectTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ProjectTwoTone"
        }
        
        
        module PropertySafetyTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PropertySafetyTwoTone"
        }
        
        
        module PushpinTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "PushpinTwoTone"
        }
        
        
        module ReconciliationTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ReconciliationTwoTone"
        }
        
        
        module RedEnvelopeTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RedEnvelopeTwoTone"
        }
        
        
        module RestTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RestTwoTone"
        }
        
        
        module RocketTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "RocketTwoTone"
        }
        
        
        module SafetyCertificateTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SafetyCertificateTwoTone"
        }
        
        
        module SaveTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SaveTwoTone"
        }
        
        
        module ScheduleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ScheduleTwoTone"
        }
        
        
        module SecurityScanTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SecurityScanTwoTone"
        }
        
        
        module SettingTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SettingTwoTone"
        }
        
        
        module ShopTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShopTwoTone"
        }
        
        
        module ShoppingTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ShoppingTwoTone"
        }
        
        
        module SkinTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SkinTwoTone"
        }
        
        
        module SmileTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SmileTwoTone"
        }
        
        
        module SoundTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SoundTwoTone"
        }
        
        
        module StarTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "StarTwoTone"
        }
        
        
        module SwitcherTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "SwitcherTwoTone"
        }
        
        
        module TabletTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TabletTwoTone"
        }
        
        
        module TagTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TagTwoTone"
        }
        
        
        module TagsTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TagsTwoTone"
        }
        
        
        module ThunderboltTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ThunderboltTwoTone"
        }
        
        
        module ToolTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "ToolTwoTone"
        }
        
        
        module TrademarkCircleTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrademarkCircleTwoTone"
        }
        
        
        module TrophyTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "TrophyTwoTone"
        }
        
        
        module UnlockTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UnlockTwoTone"
        }
        
        
        module UsbTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "UsbTwoTone"
        }
        
        
        module VideoCameraTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "VideoCameraTwoTone"
        }
        
        
        module WalletTwoTone = {
            @react.component @module("@ant-design/icons")
            external make: (
                ~className: string=?,
                ~rotate: int=?,
                ~spin: bool=?,
                ~style: ReactDOM.style=?,
                ~twoToneColor: string=?,
            ) => React.element = "WalletTwoTone"
        }
        